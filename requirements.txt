# Core (required to train/evaluate). Upper bounds guard against breaking
# major releases while leaving room for patch/minor updates.
torch>=2.1,<3
torchvision>=0.16,<1
scikit-learn>=1.3,<2
numpy>=1.24,<3
pillow>=10.0,<12
pyyaml>=6.0,<7
tqdm>=4.66,<5

# Backbones (optional): if absent, the model falls back to a small CNN so the
# code still imports and smoke-tests everywhere.
timm>=1.0,<2
open_clip_torch>=2.24,<3      # for the clip_vit_l14 backbone

# Frame extraction (optional): only needed for scripts/extract_frames.py.
opencv-python-headless>=4.8.0,<5  # headless build for servers/CI

# Data fetching (optional): only needed for src/fetch_data.py / download_latest.py.
kaggle>=1.6,<2
huggingface_hub>=0.23,<1
datasets>=2.19,<4
